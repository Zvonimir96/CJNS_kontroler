#pragma once
#include "Strip.h"

class Elements
{
public:
    static Strip house_strip;
    static Strip safe_house_strip;
    static Strip path_strip;
    static Strip dice_strip;
    static Strip touch_strip;

    static void init();
};
